// main/wifi_secrets.h.example -- 凭据模板(被 git 追踪)。
// 使用: 复制本文件为同目录 wifi_secrets.h 并填入真实 SSID/密码;
// wifi_secrets.h 已被 .gitignore 排除, 不会进入提交历史。
#pragma once

#define WIFI_SSID "YOUR_SSID"
#define WIFI_PASS "YOUR_PASSWORD"
