// main/glm_secrets.h.example -- 智谱 GLM API Key 模板(被 git 追踪)。
// 使用: 复制本文件为同目录 glm_secrets.h 并填入真实 Key(开放平台 API Key 原文,
// 即 curl 时放 Authorization 头的值, 不加 "Bearer " 前缀);
// glm_secrets.h 已被 .gitignore 排除, 不会进入提交历史。
// 留空或保持 YOUR_ 占位时固件照常编译, 主界面 GLM 区显示 NO KEY。
#pragma once

#define GLM_API_KEY "YOUR_GLM_API_KEY"
